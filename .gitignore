# Skill symlinks into the tool submodules — generated by link-skills.sh, never
# tracked (a tracked symlink dangles when its tool isn't initialized; call/0005).
/.claude/skills/

# Generated by `host-lifecycle book` (the canonical publisher); never tracked.
/book.toml
/docs/
/book/

# The Where room — bare store(s) + worktrees, materialized from .host-software by
# `host-lifecycle software --materialize`; local and never tracked (the pin in
# .host-software is the reproducibility anchor, not the tree).
/software/
